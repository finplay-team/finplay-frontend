## 관련 이슈

대응 이슈 없음 — 토스증권 벤치마킹 세션에서 발견한 버그.

## 변경 내용

댓글 삭제 확인창의 경고 문구가 사실과 달랐습니다. 백엔드는 부모 댓글을 삭제해도 실제로 지우지 않고 tombstone 처리(내용 "삭제된 댓글입니다", 작성자 "(삭제됨)"으로 치환, 대댓글은 그대로 보존)하는데, 화면은 "답글까지 함께 삭제됩니다"처럼 사실과 다르게 사용자를 겁주고 있었습니다.

문구만 고치고 끝내려 했는데, 코드를 더 보니 **화면 동작 자체도 실제로 틀려 있었습니다.**

- `src/pages/CommunityPost.tsx`: 확인창 문구를 "삭제해도 답글은 그대로 남아요"로 수정.
- **같은 파일의 `handleCommentDelete`가 여전히 "부모 삭제 = 자식까지 CASCADE 삭제"라는 낡은 전제로 동작하고 있었습니다.** 부모 댓글을 지우면 로컬 상태에서 그 부모와 대댓글을 통째로 트리에서 제거했는데, 실제로는 서버가 부모를 치환만 하므로 새로고침하면 "삭제된 댓글입니다"로 바뀐 부모와 대댓글이 다시 나타나는 로컬-서버 상태 불일치가 있었습니다. `tombstoneOrRemoveComment` 함수를 새로 만들어, 부모는 로컬에서도 내용·작성자를 서버와 똑같은 값으로 치환하고(새로고침해도 화면이 안 바뀜), 대댓글(자식)만 실제로 목록에서 제거하도록 고쳤습니다.
- `src/services/types.ts`의 `Comment` 타입 JSDoc도 같은 낡은 전제("CASCADE로 함께 사라진다")를 담고 있어 함께 정정했습니다.

## 검증

- `npx tsc -b --noEmit` 통과
- `npx vitest run` — 6개 파일 96개 테스트 전부 통과(이 페이지 자체에 기존 테스트 파일이 없어 갱신 대상 없음)

## 남은 위험 / 후속

`CommunityPost.tsx`에 대한 테스트 파일이 아예 없습니다. 이번 변경은 수동으로 로직을 검토했지만, 이 페이지 전체에 대한 테스트 커버리지가 없다는 건 별도로 다뤄볼 만합니다(이번 PR 범위 밖).

## 리뷰어에게

`tombstoneOrRemoveComment`가 백엔드 `PostCommentResponse`의 tombstone 값(`TOMBSTONED_CONTENT`·`TOMBSTONED_AUTHOR_DISPLAY`)과 정확히 일치하는지 봐주세요 — 값이 다르면 새로고침 시 화면이 깜빡이며 바뀝니다.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
